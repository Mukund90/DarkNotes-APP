apiVersion: v1
kind: Service
metadata:
  name: {{ include "darknotes.fullname" . }}-backend
  labels:
    app: backend
    {{- include "darknotes.labels" . | nindent 4 }}

spec:
  type: {{ .Values.backend.service.type }}

  selector:
    app: backend

  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.targetPort }}